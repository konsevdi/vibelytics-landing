# Vibelytics Identity Source Pass

Date: 2026-07-03
Scope: Canonical mark source, raster derivatives, social images, route logo usage, and screenshot evidence
Status: Source/export gate resolved; final visual QA and token consolidation remain open

## What Changed

- Created `brand/vibelytics-mark.svg` as the canonical editable Vibelytics mark source.
- Added `scripts/generate-brand-assets.py` as the deterministic no-dependency PNG export path.
- Regenerated:
  - `brand/vibelytics-mark.png`
  - `brand/vibelytics-x-avatar.png`
  - `brand/vibelytics-og-image.png`
  - `brand/vibelytics-x-header.png`
  - `favicon.png`
  - `apple-touch-icon.png`
  - `og-image.png`
  - `twitter-image.png`
- Updated route logo CSS in `index.html` and `pilot/index.html` to use `/brand/vibelytics-mark.svg`.
- Refreshed:
  - `docs/assets/vibelytics-landing-screenshot.png`
  - `docs/assets/vibelytics-pilot-screenshot.png`

No backend, API routes, external services, new packages, or external assets were added.

## Source Decision

The source/provenance gate is resolved for the current brand assets.

The SVG is the canonical editable mark source. PNG exports are deterministic repo-owned derivatives generated by `scripts/generate-brand-assets.py`. The exporter uses the same mark geometry as the SVG because this environment does not provide an in-sandbox SVG rasterizer.

This does not claim final production brand signoff. Final visual QA, social crop review, and shared token consolidation remain open.

## Verification Commands

```bash
python3 scripts/generate-brand-assets.py
python3 -m py_compile scripts/generate-brand-assets.py
sips -g pixelWidth -g pixelHeight brand/*.png favicon.png apple-touch-icon.png og-image.png twitter-image.png docs/assets/*.png
file brand/*.png favicon.png apple-touch-icon.png og-image.png twitter-image.png brand/vibelytics-mark.svg
npm run start
```

Playwright checks:

- Set viewport to 1440x960.
- Captured `/` as a full-page screenshot (`1425x3865`, excluding the scrollbar gutter).
- Captured `/pilot/` as a 1440x960 viewport screenshot.

Additional checks:

```bash
node -e 'JSON.parse(require("fs").readFileSync("docs/design/asset-provenance.json","utf8")); console.log("asset-provenance.json ok")'
rg -n "SR007|Speedrun" index.html
npm run build
```

## Remaining Work

Next Codex thread should run final visual QA and shared token consolidation:

1. Review `/`, `/pilot`, favicons, social images, and screenshots across desktop/mobile.
2. Consolidate duplicated route tokens across `index.html` and `pilot/index.html`.
3. Keep `docs/design/asset-provenance.json` current if any source/export changes are made.
4. Do not claim production-ready brand signoff until visual QA and token consolidation are complete.
